apiVersion: v1
kind: Service
metadata:
  name: catalogue-service
  namespace: yanashop
  labels:
    app: catalogue-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: catalogue-service